apiVersion: v1
kind: Service
metadata:
  name: o-sdk-demo-ui-svc
spec:
  selector:
    app: o-sdk-demo-ui
  ports:
    - port: 80
      targetPort: {{ .Values.ui.port }}
  type: {{ .Values.ui.serviceType }}
