# yaml-language-server: $schema=https://api.acuvity.us/api/v1/jsonschema/import.json

label: demo-agents/simple-api

apps:
  - name: simple-api
    description: "The simple API of the demo-agents repo."
    tracing: Recording
    selector:
      type: Kubernetes
      kubernetes:
        kubernetesNamespace: simple-api-demo
    components:
      - name: openai
        description: "The OpenAI API Call"
        kind: Agent
        selector:
          type: Kubernetes
          kubernetes:
            name: simple-api
            kubernetesNamespace: simple-api-demo
            type: Deployment
        egress:
          policies:
            - name: allow-openai
              description: "Allow requests to OpenAI API"
              transportMode: Proxy
              providers:
                - openai-api
              action: Allow
          defaultPolicyAction: Allow
