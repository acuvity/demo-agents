apiVersion: v1
kind: Service
metadata:
  name: langgraph-demo-ui-svc
spec:
  selector:
    app: langgraph-demo-ui
  ports:
    - port: 80
      targetPort: {{ .Values.ui.port }}
  type: {{ .Values.ui.serviceType }}
