apiVersion: v1
kind: Service
metadata:
  name: google-adk-demo-svc
spec:
  selector:
    app: google-adk-demo
  ports:
    - port: 80
      targetPort: {{ .Values.agent.port }}
  type:  {{ .Values.agent.serviceType }}
